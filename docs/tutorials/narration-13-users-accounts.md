# Narration script — "User Management: Accounts & Roster" (staff tutorial)

Video file: `users-accounts.mp4` (41 seconds, silent, on-screen captions already burned in)

Play the video muted and read each line starting around its timestamp — match the caption
on screen more than the clock.

| ~Time | Say this |
|---|---|
| 0:00 | User Management is where every account in the system lives. |
| 0:06 | Open the menu, and select User Management. |
| 0:11 | Students, staff, and outside staff each get their own table — search any of them by name or username. |
| 0:17 | Add Student opens a form for a new account. |
| 0:20 | Initials and a lunch number are all that's required — everything else has sensible defaults. |
| 0:26 | Support team roles — Case Manager, Practitioner, Professional, Group Leader — are set right here too. |
| 0:32 | The same pattern adds Staff, Outside Staff, or Admin accounts. |
| 0:36 | Share login information sends credentials to one or many accounts at once. |

## Notes on what's shown
- A pulsing orange ring circles whatever control is being talked about, and a small dot
  tracks the real cursor throughout.
- Logged in as `staff1`, an admin account — Add Staff/Outside Staff/Admin and Share login
  information are admin-only; a plain staff account only sees Add Student and the search
  tables.
- The Add Student form is opened and filled to show what's required, then closed without
  saving, so the demo data stays put for re-recording.
- All data is synthetic test data. No real students.
