# Narration script — "User Management: Student Plans" (staff tutorial)

Video file: `users-student-plans.mp4` (38 seconds, silent, on-screen captions already burned in)

Play the video muted and read each line starting around its timestamp — match the caption
on screen more than the clock.

| ~Time | Say this |
|---|---|
| 0:00 | Student Plans live right on their User Management row. |
| 0:06 | Open the menu, and select User Management. |
| 0:11 | Every student row has a menu of actions — open it with the kebab. |
| 0:17 | Add/Edit Plan opens their If/Then plan. |
| 0:21 | Write the trigger — the If — and what happens next — the Then. |
| 0:27 | Add a point-card threshold so the system can flag it automatically. |
| 0:33 | Add row starts another If/Then; Save plan keeps everything you've written. |

## Notes on what's shown
- A pulsing orange ring circles whatever control is being talked about, and a small dot
  tracks the real cursor throughout.
- Logged in as `staff25`, a plain staff account.
- The plan is left unsaved (closed instead) so the demo data stays put for re-recording.
- The "Plan Thresholds" report tile covered in the Reports videos is where a saved
  threshold shows up once it's met.
- All data is synthetic test data. No real students.
