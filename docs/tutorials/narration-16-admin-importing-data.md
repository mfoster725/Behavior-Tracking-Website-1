# Narration script — "Admin: Importing Data" (staff tutorial)

Video file: `admin-importing-data.mp4` (44 seconds, silent, on-screen captions already burned in)

Play the video muted and read each line starting around its timestamp — match the caption
on screen more than the clock.

| ~Time | Say this |
|---|---|
| 0:00 | There are two ways to bulk-load accounts — a one-time CSV, or an ongoing Google Sheet. |
| 0:06 | Open the menu, and select Admin Panel. |
| 0:11 | Import Users from CSV bulk-creates or updates staff, outside staff, or students. |
| 0:16 | Pick the import type — the required columns update to match. |
| 0:23 | Existing accounts are matched and updated — by User Number for staff, Lunch Number for students — instead of duplicated. |
| 0:29 | Google Sheet Sync keeps a spreadsheet and the website in step automatically. |
| 0:34 | Check Setup tells you how many sheet rows aren't on the website yet. |
| 0:39 | Pull brings sheet edits in; Push writes website changes out; Sync Both Ways does both, in order. |

## Notes on what's shown
- A pulsing orange ring circles whatever control is being talked about, and a small dot
  tracks the real cursor throughout.
- Logged in as `staff1`, an admin account — the whole Admin Panel is admin-only.
- No file is actually chosen or imported, and none of the Google Sheet buttons are clicked
  — CSV import and sheet sync both change real account data, so this only tours the UI.
- All data is synthetic test data. No real students.
