# Narration script — "Reports: Plan Thresholds Drill-Down" (staff tutorial)

Video file: `reports-plan-thresholds.mp4` (36 seconds, silent, on-screen captions already burned in)

Play the video muted and read each line starting around its timestamp — match the caption
on screen more than the clock.

| ~Time | Say this |
|---|---|
| 0:00 | Plan Thresholds is one number — the drill-down is where the real detail lives. |
| 0:06 | Open Reports and pick one student. |
| 0:13 | Plan Thresholds counts how many times an If/Then plan condition was met. |
| 0:18 | Click the tile to see which If was met, how many times, and whether it was delivered. |
| 0:27 | For a group instead of one student, this same card breaks it down by student too. |
| 0:31 | Click the tile again to close it. |

## Notes on what's shown
- A pulsing orange ring circles whatever control is being talked about, and a small dot
  tracks the real cursor throughout.
- Logged in as `staff25`, viewing Test Student 1 with the timeframe set to "All Time".
- `scripts/tutorial-videos/seed_plan_thresholds_demo.py` gives this student a real plan
  with two If/Then rows and seven backdated threshold-met events — seed_test_data.py alone
  leaves every student with no plan at all. See "User Management: Student Plans" for how
  to write a plan like this from scratch.
- All data is synthetic test data. No real students.
