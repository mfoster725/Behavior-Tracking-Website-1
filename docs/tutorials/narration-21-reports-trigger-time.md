# Narration script — "Reports: Trigger Time Drill-Down" (staff tutorial)

Video file: `reports-trigger-time.mp4` (39 seconds, silent, on-screen captions already burned in)

Play the video muted and read each line starting around its timestamp — match the caption
on screen more than the clock.

| ~Time | Say this |
|---|---|
| 0:00 | The Trigger Time heat map is a starting point — every square opens its own detail. |
| 0:06 | Open Reports and pick one student. |
| 0:13 | Trigger Time is the single day-and-period combination with the most infractions. |
| 0:19 | Click the tile for the full heat map, or switch to Table. |
| 0:26 | Click any time slot in the table for that slot's own detail. |
| 0:34 | That opens its own tab — close it when you're done, then click the tile again to close the whole card. |

## Notes on what's shown
- A pulsing orange ring circles whatever control is being talked about, and a small dot
  tracks the real cursor throughout.
- Logged in as `staff25`, viewing Test Student 1 with the timeframe set to "All Time".
- The time-slot detail shown here breaks frenzy severity and purpose down for that one
  slot — the same click works for any square in the table or the graph heat map.
- All data is synthetic test data. No real students.
