# Narration script — "Reports: Incidents Drill-Down" (staff tutorial)

Video file: `reports-incidents.mp4` (41 seconds, silent, on-screen captions already burned in)

Play the video muted and read each line starting around its timestamp — match the caption
on screen more than the clock.

| ~Time | Say this |
|---|---|
| 0:00 | Reminders and Resets each get their own drill-down, side by side. |
| 0:06 | Open Reports and pick one student. |
| 0:13 | Incidents splits into Reminders and Resets. |
| 0:18 | Click Reminders to drill into just that bar. |
| 0:24 | Table view lists each one by day, instead of just a running total. |
| 0:31 | Resets get the exact same breakdown — click it for its own card. |
| 0:38 | Click either bar again to close its card. |

## Notes on what's shown
- A pulsing orange ring circles whatever control is being talked about, and a small dot
  tracks the real cursor throughout.
- Logged in as `staff25`, viewing Test Student 1 with the timeframe set to "All Time".
- Reminders and Resets open as two independent cards — closing one leaves the other open.
- All data is synthetic test data. No real students.
