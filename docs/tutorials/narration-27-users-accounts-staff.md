# Narration script — "User Management: Accounts & Roster" (plain staff tutorial)

Video file: `users-accounts-staff.mp4` (37 seconds, silent, on-screen captions already burned in)

Play the video muted and read each line starting around its timestamp — match the caption
on screen more than the clock.

See also `narration-13-users-accounts.md` (admin account) and
`narration-28-users-accounts-outside-staff.md` (Outside Staff account) for the other two
roles' views of this same page.

| ~Time | Say this |
|---|---|
| 0:00 | Here's User Management from a plain staff account, not an admin. |
| 0:06 | Open the menu, and select User Management. |
| 0:11 | As a plain staff account, you can search students, staff, and outside staff by name or username. |
| 0:18 | Add Student sits right at the top of the list, so it's easy to find. |
| 0:21 | Initials and a lunch number are required, and so are a student email and a parent or guardian email. |
| 0:28 | Staff accounts can view Staff, Outside Staff, and Admin lists, but only an admin can add or edit those accounts. |

## Notes on what's shown
- A pulsing orange ring circles whatever control is being talked about, and a small dot
  tracks the real cursor throughout.
- Logged in as `staff25`, a plain staff account (not admin, not Outside Staff). It sees
  Add Student (at the top of the Current Students list) and every table, but never the
  Add Staff/Outside Staff/Admin buttons or Share login information — those stay admin-only.
- Student email and parent/guardian email are both required fields now (marked with `*`)
  — the form won't save without them.
- The Add Student form is opened and filled to show what's required, then closed without
  saving, so the demo data stays put for re-recording.
- All data is synthetic test data. No real students.
