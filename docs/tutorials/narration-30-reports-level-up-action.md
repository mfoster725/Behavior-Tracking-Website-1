# Narration script — "Reports: Leveling a Student Up" (staff tutorial)

Video file: `reports-level-up-action.mp4` (35 seconds, silent, on-screen captions already burned in)

Play the video muted and read each line starting around its timestamp — match the caption
on screen more than the clock.

| ~Time | Say this |
|---|---|
| 0:00 | The Level Up's drill-down shows who's ready — this is where you actually level them up. |
| 0:06 | Open Reports for your whole caseload — same place the Level Up's tile lives. |
| 0:12 | Click the Level Up's tile to open the readiness tables. |
| 0:16 | A student who's already qualified shows "Eligible now!" with a Level Up button — only for admins and Case Managers. |
| 0:22 | Clicking it asks you to confirm — this actually promotes them and restarts their 30-day window. |
| 0:29 | They're promoted immediately and drop out of this table — no separate approval step. |

## Notes on what's shown
- A pulsing orange ring circles whatever control is being talked about, and a small dot
  tracks the real cursor throughout.
- Logged in as `staff21`, the student's actual Case Manager — only an admin or the Case
  Manager on a student's team sees the Level Up button at all. Everyone else just sees the
  "Eligible now!" text with nothing to click.
- The confirm dialog is the browser's own native prompt, not the app's usual popup style —
  that's intentional, not a bug.
- This is a real, logged, one-way action: their card color changes and their 30-day
  qualifying window resets to zero, immediately, with no undo button here.
- All data is synthetic test data. No real students.
